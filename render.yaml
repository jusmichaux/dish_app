services:
  - type: web
    name: dish-finder
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: flet run main.py --web --port $PORT --host 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0